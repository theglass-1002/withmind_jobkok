# 잡콕(JobKok) 프론트엔드 개발 산출물

## 1. 프로젝트 개요

### 1.1 프로젝트 정보
- **프로젝트명**: JobKok (잡콕)
- **프로젝트 설명**: AI 기반 채용 매칭 및 모의면접 서비스 웹 애플리케이션
- **개발 기간**: 2024년 8월 ~ 2025년 5월 (약 10개월)
- **프로젝트 URL**: https://jobkok.kr
- **API 서버**: https://api.jobkok.kr
- **AI API 서버**: https://ai.api.jobkok.kr

### 1.2 프로젝트 목표
- AI 기반 채용공고 추천 시스템
- 실시간 AI 모의면접 기능 제공
- 이력서 작성 및 관리 시스템
- 기업-구직자 매칭 플랫폼

---

## 2. 기술 스택

### 2.1 코어 기술
- **언어**: TypeScript 5.8.3
- **프레임워크**: React 19.1.1
- **빌드 도구**: Vite 7.1.2
- **라우팅**: React Router DOM 7.8.1

### 2.2 UI/UX 라이브러리
- **디자인 시스템**: Bootstrap 5.3.8, React Bootstrap 2.10.10
- **차트**: Chart.js 4.5.1, react-chartjs-2 5.3.1
- **폼 컴포넌트**: 
  - react-select 5.10.2 (드롭다운)
  - rc-slider 11.1.9 (슬라이더)
  - react-switch 7.1.0 (토글 스위치)
- **반응형 디자인**: react-responsive 10.0.1
- **알림**: react-toastify 11.0.5

### 2.3 API 및 상태 관리
- **HTTP 클라이언트**: Axios 1.13.2
- **인증**: JWT (jwt-decode 4.0.0)
- **로컬 스토리지**: LocalStorage, SessionStorage

### 2.4 문서 처리
- **PDF 생성**: jsPDF 3.0.4
- **캔버스**: html2canvas 1.4.1

### 2.5 개발 도구
- **린트**: ESLint 9.33.0, TypeScript ESLint 8.39.1
- **타입 체킹**: TypeScript 5.8.3
- **경로 별칭**: vite-tsconfig-paths 5.1.4

### 2.6 배포 및 호스팅
- **호스팅**: Firebase Hosting
- **빌드**: Vite
- **CI/CD**: Firebase CLI를 통한 자동 배포

---

## 3. 프로젝트 구조

### 3.1 폴더 구조 트리

```
jobkok-web/
├── public/                      # 정적 파일
├── src/
│   ├── api/                     # API 레이어
│   │   ├── auth/                # 인증 API
│   │   │   ├── auth.api.ts
│   │   │   └── auth.types.ts
│   │   ├── resume/              # 이력서 API
│   │   │   ├── resume.api.ts
│   │   │   └── resume.types.ts
│   │   ├── job/                 # 채용공고 API
│   │   │   ├── job.api.ts
│   │   │   └── job.types.ts
│   │   ├── interview/           # 모의면접 API
│   │   │   ├── interview.api.ts
│   │   │   └── interview.types.ts
│   │   ├── report/              # 면접 리포트 API
│   │   │   ├── report.api.ts
│   │   │   └── report.types.ts
│   │   ├── recommendation/      # 추천 API
│   │   │   ├── recommendation.api.ts
│   │   │   └── recommendation.types.ts
│   │   ├── support/             # 고객지원 API
│   │   │   ├── support.api.ts
│   │   │   └── support.types.ts
│   │   ├── company/             # 기업 회원 API
│   │   │   ├── auth/            # 기업 인증
│   │   │   └── job/             # 기업 채용공고
│   │   ├── axios.instance.ts    # Axios 인스턴스 설정
│   │   └── fileUpload.api.ts    # 파일 업로드 API
│   │
│   ├── app/                     # 앱 설정 및 레이아웃
│   │   ├── Layout.tsx           # 전역 레이아웃 컴포넌트
│   │   ├── LayoutContext.tsx    # 레이아웃 Context
│   │   ├── MaintenanceGuard.tsx # 점검 모드 가드
│   │   └── router.tsx           # 라우팅 설정
│   │
│   ├── pages/                   # 페이지 컴포넌트
│   │   ├── Home/                # 메인 페이지
│   │   ├── Auth/                # 인증 페이지
│   │   │   ├── Login/           # 로그인
│   │   │   ├── Signup/          # 회원가입
│   │   │   ├── Consent/         # 소셜 로그인 동의
│   │   │   └── Recovery/        # 계정 복구
│   │   ├── Jobs/                # 채용공고
│   │   │   ├── JobsList.tsx
│   │   │   ├── JobDetail.tsx
│   │   │   └── RecentJobs/
│   │   ├── Resume/              # 이력서
│   │   │   ├── ResumeList.tsx
│   │   │   ├── ResumeCreate/    # 이력서 작성
│   │   │   ├── ResumeEdit/      # 이력서 수정
│   │   │   └── ResumeDetail/    # 이력서 상세
│   │   ├── MockInterview/       # 모의면접
│   │   │   ├── MockInterviewGuide/    # 가이드
│   │   │   ├── MockInstructions/      # 사용 안내
│   │   │   ├── MockSettings/          # 환경 설정
│   │   │   └── MockInterviewLive/     # 실시간 면접
│   │   ├── InterviewReport/     # 면접 리포트
│   │   │   ├── InterviewReport.tsx
│   │   │   ├── analysis/        # 분석 결과
│   │   │   ├── my-report/       # 내 리포트
│   │   │   └── history/         # 이력
│   │   ├── Mypage/              # 마이페이지
│   │   │   ├── Mypage.tsx
│   │   │   ├── EditProfile/     # 프로필 수정
│   │   │   ├── Plan/            # 플랜 관리
│   │   │   ├── Postings/        # 공고 관리
│   │   │   └── Support/         # 고객지원
│   │   ├── Company/             # 기업 회원
│   │   │   ├── login/           # 기업 로그인
│   │   │   ├── signup/          # 기업 회원가입
│   │   │   └── dashboard/       # 기업 대시보드
│   │   │       ├── Guide/
│   │   │       ├── AIMatching/  # AI 매칭
│   │   │       ├── MatchHistory/
│   │   │       ├── Statistics/
│   │   │       └── Pricing/
│   │   ├── Purchase/            # 결제
│   │   ├── PurchaseResult/      # 결제 결과
│   │   ├── Docs/                # 약관/정책
│   │   ├── ComingSoon/          # 서비스 준비 중
│   │   └── NotFound.tsx         # 404 페이지
│   │
│   ├── shared/                  # 공유 리소스
│   │   ├── components/          # 공통 컴포넌트
│   │   │   ├── Navbar.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── Pagination.tsx
│   │   │   ├── tabs/
│   │   │   ├── form/            # 폼 컴포넌트
│   │   │   ├── calendar/        # 달력 컴포넌트
│   │   │   ├── loading/         # 로딩 컴포넌트
│   │   │   ├── tooltip/
│   │   │   ├── job-role-picker/
│   │   │   ├── location-picker/
│   │   │   ├── education-picker/
│   │   │   └── job-posting-item/
│   │   ├── utils/               # 유틸리티 함수
│   │   │   ├── StorageManager.ts
│   │   │   ├── validators.ts
│   │   │   ├── util.ts
│   │   │   └── mockInterviewGuard.ts
│   │   ├── api/                 # 공통 API
│   │   ├── lib/                 # 라이브러리 설정
│   │   │   └── toastConfig.ts
│   │   └── styles/              # 공통 스타일
│   │
│   ├── features/                # 기능별 컴포넌트
│   │   └── jobs/
│   │       └── JobCard.tsx
│   │
│   ├── assets/                  # 정적 리소스
│   │   ├── backgrounds/
│   │   ├── fonts/
│   │   ├── icons/
│   │   ├── illustrations/
│   │   ├── images/
│   │   └── progress/
│   │
│   ├── config/                  # 설정 파일
│   │   └── config.ts            # 환경 설정
│   │
│   ├── data/                    # 정적 데이터
│   │
│   ├── types/                   # 타입 정의
│   │   └── global.d.ts
│   │
│   ├── main.tsx                 # 앱 진입점
│   └── vite-env.d.ts
│
├── .firebase/                   # Firebase 설정
├── dist/                        # 빌드 결과물
├── firebase.json                # Firebase 호스팅 설정
├── vite.config.ts               # Vite 설정
├── tsconfig.json                # TypeScript 설정
├── package.json                 # 의존성 관리
└── README.md                    # 프로젝트 README
```

### 3.2 코드 규모
- **총 코드 라인 수**: 약 74,642 라인
- **주요 파일 형식**: TypeScript (.ts, .tsx)

---

## 4. 주요 기능

### 4.1 인증 시스템
- **일반 회원 로그인/회원가입**
  - 이메일 기반 회원가입 (본인인증 포함)
  - 소셜 로그인 (카카오, 네이버, 구글)
  - JWT 기반 토큰 인증
  - 자동 토큰 갱신 (Refresh Token)
  
- **기업 회원 로그인/회원가입**
  - 기업 정보 등록
  - 별도의 기업 인증 시스템
  - 기업용 대시보드

- **계정 관리**
  - 비밀번호 찾기/재설정
  - 로그인 상태 유지 (자동 로그인)

### 4.2 이력서 관리
- **이력서 작성**
  - 단계별 이력서 작성 프로세스
  - 기본 정보 (이름, 연락처, 사진 업로드)
  - 학력 정보
  - 경력 정보
  - 기술 스택 (Hard Skills)
  - 소프트 스킬
  - 자기소개서
  - 희망 직무 및 지역
  - 수상 및 자격증
  - 포트폴리오/문서 업로드
  - 대외활동
  
- **이력서 목록/상세/수정**
  - 여러 이력서 관리
  - 이력서 상세 보기
  - 이력서 수정 기능
  - PDF 다운로드

### 4.3 채용공고
- **공고 목록 및 검색**
  - 전체 채용공고 리스트
  - 필터링 (직무, 지역, 경력, 학력 등)
  - 검색 기능
  - 페이지네이션
  
- **공고 상세**
  - 공고 상세 정보 확인
  - 북마크 기능
  - 지원하기 (이력서 연동)
  
- **AI 기반 추천**
  - 사용자 이력서 기반 공고 추천
  - 매칭 점수 표시

### 4.4 AI 모의면접
- **모의면접 설정**
  - 환경 테스트 (마이크, 카메라)
  - 이력서 선택
  - 면접 난이도 설정
  
- **실시간 모의면접**
  - AI 면접관과의 실시간 대화
  - 음성 인식 및 응답
  - 면접 진행 상태 표시
  - 실시간 피드백
  
- **면접 리포트**
  - 면접 결과 분석
  - 답변 평가 (정확성, 논리성, 태도 등)
  - 개선 제안
  - 차트 및 통계
  - PDF 다운로드

### 4.5 기업 대시보드
- **AI 매칭**
  - 채용공고 등록
  - AI 기반 인재 매칭
  - 매칭 리포트 확인
  
- **통계**
  - 공고별 조회수
  - 지원자 통계
  - 매칭 성공률
  
- **매칭 히스토리**
  - 과거 매칭 내역
  - 지원자 정보 확인

### 4.6 마이페이지
- **프로필 관리**
  - 개인정보 수정
  - 프로필 사진 변경
  
- **플랜 관리**
  - 구독 플랜 확인
  - 결제 내역
  
- **공고 관리**
  - 북마크한 공고
  - 지원한 공고
  - 마감된 공고
  
- **고객 지원**
  - FAQ
  - 1:1 문의
  - 공지사항
  - 공고 신고

### 4.7 결제 시스템
- **플랜 구매**
  - 다양한 플랜 선택
  - 결제 페이지
  - 결제 성공/실패 페이지

### 4.8 반응형 디자인
- **데스크톱/모바일 최적화**
  - 반응형 레이아웃
  - 모바일 전용 네비게이션
  - 터치 인터랙션 지원

---

## 5. API 연동

### 5.1 REST API 구조

#### 5.1.1 기본 설정
- **Base URL**: `https://api.jobkok.kr`
- **AI Base URL**: `https://ai.api.jobkok.kr`
- **인증 방식**: Bearer Token (JWT)
- **헤더**: `Authorization: Bearer {accessToken}`

#### 5.1.2 토큰 관리
- **Access Token**: API 요청 인증
- **Refresh Token**: Access Token 갱신
- **자동 갱신**: 401/403 에러 시 자동 refresh
- **저장소**: LocalStorage / SessionStorage

### 5.2 주요 API 엔드포인트

#### 인증 API (`/api/auth/`)
```typescript
POST /auth/login              // 로그인
POST /auth/signup             // 회원가입
POST /auth/refresh            // 토큰 갱신
POST /auth/logout             // 로그아웃
GET  /auth/oauth/kakao        // 카카오 로그인
GET  /auth/oauth/naver        // 네이버 로그인
GET  /auth/oauth/google       // 구글 로그인
POST /auth/recovery           // 비밀번호 찾기
```

#### 이력서 API (`/api/resume/`)
```typescript
GET    /resume                // 이력서 목록
POST   /resume                // 이력서 생성
GET    /resume/:id            // 이력서 상세
PUT    /resume/:id            // 이력서 수정
DELETE /resume/:id            // 이력서 삭제
```

#### 채용공고 API (`/api/job/`)
```typescript
GET    /job                   // 공고 목록
GET    /job/:id               // 공고 상세
POST   /job/bookmark          // 북마크 추가
DELETE /job/bookmark/:id      // 북마크 삭제
GET    /job/recent            // 최근 본 공고
```

#### 모의면접 API (`/api/interview/`)
```typescript
POST /interview/start         // 면접 시작
POST /interview/answer        // 답변 제출
POST /interview/end           // 면접 종료
GET  /interview/:id           // 면접 상세
```

#### 면접 리포트 API (`/api/report/`)
```typescript
GET /report/:id               // 리포트 상세
GET /report/list              // 리포트 목록
GET /report/:id/pdf           // PDF 다운로드
```

#### 추천 API (`/api/recommendation/`)
```typescript
GET /recommendation/jobs      // 추천 공고
GET /recommendation/match     // 매칭 점수
```

#### 고객지원 API (`/api/support/`)
```typescript
GET  /support/faq             // FAQ 목록
GET  /support/notices         // 공지사항 목록
POST /support/inquiry         // 문의 등록
GET  /support/inquiry         // 문의 목록
```

#### 기업 API (`/api/company/`)
```typescript
POST /company/auth/login      // 기업 로그인
POST /company/auth/signup     // 기업 회원가입
GET  /company/job             // 기업 공고 목록
POST /company/job             // 공고 등록
GET  /company/matching        // AI 매칭 결과
```

### 5.3 API 응답 형식

#### 성공 응답
```typescript
{
  code: 200,
  msg: "SUCCESS",
  data: { ... }
}
```

#### 에러 응답
```typescript
{
  code: 400,
  msg: "ERROR_MESSAGE"
}
```

### 5.4 에러 처리
- **401 Unauthorized**: 토큰 만료 → 자동 Refresh Token 갱신
- **403 Forbidden**: 권한 없음 → 로그인 페이지 리다이렉트
- **503 Service Unavailable**: 점검 중 → ComingSoon 페이지
- **999 Custom Error**: NO_TOKEN, TOKEN_REFRESH_FAILED

### 5.5 이벤트 추적 시스템
Axios 인터셉터를 통한 자동 이벤트 수집:
- **API 호출 추적**: 모든 API 요청/응답 시간 기록
- **클릭 이벤트**: 버튼, 링크 클릭 추적
- **페이지 뷰**: 페이지 전환 추적
- **에러 추적**: JS 에러, API 에러 자동 수집
- **배치 전송**: 10초마다 또는 페이지 이탈 시 전송

---

## 6. 설치 및 실행 방법

### 6.1 개발 환경 요구사항
- **Node.js**: 18.x 이상 권장
- **npm** 또는 **yarn**

### 6.2 설치

```bash
# 저장소 클론
git clone <repository-url>
cd jobkok-web

# 의존성 설치
npm install
```

### 6.3 환경 설정

`src/config/config.ts` 파일에서 환경 변수 설정:

```typescript
export const IsDev = false;  // 개발 모드 여부

export const LOCAL_BASE_URL = "http://localhost:5173";
export const REAL_BASE_URL  = "https://jobkok.kr";

export const API_BASE_URL = "https://api.jobkok.kr";
export const AI_BASE_URL = "https://ai.api.jobkok.kr";

// 소셜 로그인 설정
export const NAVER_CLIENT_ID = "...";
export const KAKAO_REST_API_KEY = "...";
export const GOOGLE_CLIENT_ID = "...";
```

### 6.4 개발 서버 실행

```bash
npm run dev
```

- 브라우저에서 `http://localhost:5173` 접속
- `--host` 옵션으로 외부 접속 가능

### 6.5 린트 실행

```bash
npm run lint
```

---

## 7. 빌드 및 배포 가이드

### 7.1 프로덕션 빌드

```bash
npm run build
```

- TypeScript 컴파일 (`tsc -b`)
- Vite 번들링
- 결과물: `dist/` 폴더

### 7.2 Firebase 호스팅 배포

#### 7.2.1 Firebase 설정

`firebase.json`:
```json
{
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
```

#### 7.2.2 배포 명령어

```bash
# 빌드 + 배포
npm run deploy

# 또는 수동 실행
npm run build
firebase deploy --only hosting
```

### 7.3 빌드 최적화
- **코드 스플리팅**: React Router 기반 자동 분할
- **트리 쉐이킹**: Vite의 Rollup 기반 최적화
- **이미지 최적화**: 정적 자산 최적화
- **빌드 시간 주입**: Vite 플러그인으로 빌드 시간 자동 삽입

### 7.4 배포 전 체크리스트
- [ ] `config.ts`의 `IsDev` = `false` 확인
- [ ] API URL이 프로덕션 서버로 설정되어 있는지 확인
- [ ] 린트 에러 없는지 확인 (`npm run lint`)
- [ ] 빌드 성공 확인 (`npm run build`)
- [ ] 로컬에서 빌드 결과물 테스트 (`npx serve dist`)

---

## 8. 주요 구현 상세

### 8.1 라우팅 구조

#### 8.1.1 React Router 설정
- **파일**: `src/app/router.tsx`
- **라우터**: `createBrowserRouter`
- **레이아웃**: 모든 페이지에 `<Layout>` 적용
- **중첩 라우팅**: Mypage, Company 등에서 활용

#### 8.1.2 주요 라우트

```typescript
/                              → Home
/jobs                          → JobsList
/jobs/:jobId                   → JobDetail
/resumes                       → ResumeList
/resumes/create                → ResumeCreate
/resumes/:resumeId             → ResumeDetail
/resumes/:resumeId/edit        → ResumeEdit
/mock-interview/guide          → MockInterviewGuide
/mock-interview/settings       → MockSettings
/mock-interview/mock-interview-live → MockInterviewLive
/mock-interview-report         → InterviewReport
/mock-interview/analysis/:id   → MockAnalysisPage
/mypage                        → Mypage (중첩 라우팅)
/company                       → CompanyDashboard (중첩 라우팅)
/login                         → Login
/signup                        → Signup
/purchase                      → PurchaseLayout
```

#### 8.1.3 보호된 라우트
- 로그인 필요 페이지: 이력서, 모의면접, 마이페이지 등
- 토큰 검증: Axios 인터셉터에서 자동 처리

### 8.2 레이아웃 시스템

#### 8.2.1 Layout 컴포넌트
- **파일**: `src/app/Layout.tsx`
- **구성**: Header, Footer, BottomNav
- **동적 제어**: props로 각 요소 표시/숨김 제어

```typescript
<Layout 
  screen="ResumeDetail" 
  showFooter="desktop-only" 
  showBottomNav={false}
>
  <ResumeDetail />
</Layout>
```

#### 8.2.2 LayoutContext
- **파일**: `src/app/LayoutContext.tsx`
- **전역 상태**: 레이아웃 설정 공유

#### 8.2.3 MaintenanceGuard
- **파일**: `src/app/MaintenanceGuard.tsx`
- **기능**: 503 에러 시 점검 페이지로 리다이렉트

### 8.3 API 레이어

#### 8.3.1 Axios 인스턴스 (`axios.instance.ts`)

**주요 기능**:
1. **자동 토큰 주입**: 모든 요청에 Bearer Token 자동 추가
2. **토큰 갱신**: 401/403 에러 시 Refresh Token으로 자동 갱신
3. **이벤트 추적**: API 호출, 클릭, 에러 자동 수집
4. **에러 핸들링**: 통일된 에러 응답 형식

**Request Interceptor**:
```typescript
- 토큰 타입 결정 (user / company)
- Authorization 헤더 추가
- 요청 시작 시간 기록
```

**Response Interceptor**:
```typescript
- 성공 시: 이벤트 큐에 API 호출 기록
- 에러 시:
  - 503: 점검 페이지 리다이렉트
  - 401/403: Refresh Token으로 재시도
  - 기타: 통일된 에러 객체 반환
```

#### 8.3.2 API 모듈화

각 도메인별로 API 파일 분리:
- `auth.api.ts`: 로그인, 회원가입, 로그아웃
- `resume.api.ts`: 이력서 CRUD
- `job.api.ts`: 채용공고 조회, 북마크
- `interview.api.ts`: 모의면접 진행
- `report.api.ts`: 면접 리포트 조회

**타입 안정성**:
- 각 API마다 `.types.ts` 파일로 타입 정의
- 요청/응답 타입 명확히 정의

### 8.4 상태 관리

#### 8.4.1 LocalStorage 관리
- **파일**: `src/shared/utils/StorageManager.ts`
- **토큰 저장**: accessToken, refreshToken
- **기업 토큰**: companyAccessToken, companyRefreshToken
- **자동 로그인**: 로그인 상태 유지

#### 8.4.2 Context API
- **LayoutContext**: 레이아웃 설정
- 필요 시 추가 Context 생성 가능

### 8.5 공통 컴포넌트

#### 8.5.1 폼 컴포넌트
- **FormField**: 입력 필드 + 레이블 + 에러 메시지
- **FormInput**: 커스텀 인풋
- **DateInline**: 인라인 날짜 선택
- **GenderChoice**: 성별 선택

#### 8.5.2 피커 컴포넌트
- **ModalJobRolePicker**: 직무 선택 모달
- **ModalLocationPicker**: 지역 선택 모달
- **ModalEducationPicker**: 학력 선택 모달
- **ModalCareerRangePicker**: 경력 범위 선택

#### 8.5.3 레이아웃 컴포넌트
- **Navbar**: 상단 네비게이션
- **Footer**: 푸터
- **Pagination**: 페이지네이션
- **Tabs**: 탭 네비게이션

#### 8.5.4 기타
- **LoadingOverlay**: 로딩 오버레이
- **Tooltip**: 툴팁
- **SelectDropdown**: 드롭다운

### 8.6 유틸리티 함수

#### `validators.ts`
- 이메일 검증
- 비밀번호 검증
- 전화번호 검증
- 필수 입력 검증

#### `util.ts`
- 날짜 포맷팅
- 문자열 처리
- 파일 크기 변환

#### `mockInterviewGuard.ts`
- 모의면접 진입 전 이력서 검증
- 환경 설정 확인

### 8.7 이력서 작성 시스템

#### 8.7.1 섹션별 컴포넌트
`src/pages/Resume/ResumeCreate/` 하위:

1. **BasicInfoSection**: 기본 정보 (이름, 연락처, 사진)
2. **EducationSection**: 학력
3. **CareerSection**: 경력
4. **HardSkillSection**: 기술 스택
5. **SoftSkillsSection**: 소프트 스킬
6. **SelfIntroductionSection**: 자기소개
7. **DesiredRoleSection**: 희망 직무
8. **LocationSection**: 희망 근무지
9. **AwardsCertificationsSection**: 수상/자격증
10. **PortfolioDocumentsSection**: 포트폴리오/문서
11. **ActivitiesSection**: 대외활동

#### 8.7.2 폼 관리
- 각 섹션마다 `Form/` 하위에 입력 폼 컴포넌트
- 단계별 저장 및 검증
- 파일 업로드 API 연동

### 8.8 모의면접 시스템

#### 8.8.1 환경 설정
- **MockSettings**: 이력서 선택, 난이도 설정
- **EnvironmentTestView**: 마이크/카메라 테스트
- 실시간 음성 인식 테스트

#### 8.8.2 실시간 면접
- **MockInterviewLive**: WebRTC 기반 실시간 면접
- AI 면접관 음성 출력
- 사용자 음성 인식 및 텍스트 변환
- 면접 진행 상태 표시

#### 8.8.3 면접 리포트
- **MockAnalysisPage**: 면접 분석 결과
- Chart.js를 활용한 데이터 시각화
- 답변별 평가 및 피드백
- PDF 다운로드 (jsPDF + html2canvas)

---

## 9. 개발 중 이슈 및 해결 방법

### 9.1 토큰 갱신 무한 루프 문제

**문제**:
- 401 에러 발생 시 Refresh Token 갱신 시도
- 갱신 실패 시 무한 루프 발생

**해결**:
```typescript
// axios.instance.ts
if (!originalRequest._retry) {
  originalRequest._retry = true;
  // Refresh Token 갱신 로직
}
```
- `_retry` 플래그로 재시도 1회 제한

### 9.2 소셜 로그인 리다이렉트 처리

**문제**:
- 카카오/네이버/구글 OAuth 콜백 URL 처리
- 토큰 추출 및 저장

**해결**:
```typescript
// router.tsx
{ 
  path: "/auth/oauth/kakao/callback", 
  element: <Login /> 
}
```
- 콜백 URL을 Login 컴포넌트로 라우팅
- URL 파라미터에서 토큰 추출 후 저장

### 9.3 이력서 파일 업로드 크기 제한

**문제**:
- 대용량 파일 업로드 시 서버 에러

**해결**:
- 클라이언트에서 사전 검증 (5MB 이하)
- 파일 크기 초과 시 Toast 알림
- 이미지 압축 처리

### 9.4 모바일 환경에서 마이크 권한

**문제**:
- 모바일 브라우저에서 마이크 권한 요청 실패
- HTTPS 필수

**해결**:
- Firebase Hosting으로 HTTPS 배포
- 권한 요청 전 사용자 안내 모달 표시

### 9.5 페이지 새로고침 시 404 에러

**문제**:
- SPA 라우팅에서 새로고침 시 404 발생

**해결**:
```json
// firebase.json
{
  "hosting": {
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
```
- 모든 경로를 `index.html`로 리다이렉트

### 9.6 점검 모드 처리

**문제**:
- 서버 점검 시 사용자 안내 필요

**해결**:
- API에서 503 상태 코드 반환
- Axios 인터셉터에서 자동 감지
- ComingSoon 페이지로 리다이렉트

### 9.7 기업/사용자 이중 인증 시스템

**문제**:
- 일반 회원과 기업 회원의 토큰 분리 필요

**해결**:
- 토큰 타입 구분 (`tokenType: "user" | "company"`)
- LocalStorage에 별도 키로 저장
- API 요청 시 동적으로 토큰 선택

### 9.8 빌드 시간 자동 업데이트

**문제**:
- 배포 시간 추적 필요

**해결**:
```typescript
// vite.config.ts
const injectBuildDate = (): Plugin => ({
  name: 'inject-build-date',
  transformIndexHtml(html) {
    const buildDate = new Date().toLocaleString('sv-SE', {...});
    return html.replace(
      /<meta name="build-date" content="[^"]*" \/>/,
      `<meta name="build-date" content="${buildDate}" />`
    );
  },
})
```
- Vite 플러그인으로 빌드 시간 자동 주입

---

## 10. 테스트 가이드

### 10.1 개발 환경 테스트

```bash
# 개발 서버 실행
npm run dev

# 로컬 테스트
http://localhost:5173
```

### 10.2 빌드 결과물 테스트

```bash
# 프로덕션 빌드
npm run build

# 빌드 결과물 서빙
npx serve dist

# 테스트
http://localhost:3000
```

### 10.3 주요 테스트 시나리오

#### 10.3.1 인증 플로우
1. 회원가입 → 본인인증 → 로그인
2. 소셜 로그인 (카카오/네이버/구글)
3. 자동 로그인 확인
4. 토큰 만료 후 자동 갱신 확인

#### 10.3.2 이력서 작성
1. 각 섹션 입력 및 저장
2. 파일 업로드 (프로필 사진, 포트폴리오)
3. 이력서 목록 확인
4. 이력서 수정
5. 이력서 상세 보기

#### 10.3.3 모의면접
1. 이력서 선택
2. 환경 테스트 (마이크/카메라)
3. 실시간 면접 진행
4. 면접 리포트 확인
5. PDF 다운로드

#### 10.3.4 채용공고
1. 공고 목록 조회
2. 필터링 및 검색
3. 공고 상세 보기
4. 북마크 추가/삭제
5. AI 추천 공고 확인

#### 10.3.5 기업 기능
1. 기업 로그인
2. 채용공고 등록
3. AI 매칭 실행
4. 매칭 리포트 확인
5. 통계 확인

### 10.4 브라우저 호환성 테스트
- **Chrome**: 최신 버전
- **Safari**: 최신 버전
- **Firefox**: 최신 버전
- **모바일**: iOS Safari, Android Chrome

### 10.5 반응형 테스트
- **데스크톱**: 1920px, 1440px
- **태블릿**: 768px, 1024px
- **모바일**: 375px, 414px

---

## 11. 향후 개선 계획

### 11.1 기능 개선
- [ ] 실시간 채팅 시스템 (기업-구직자)
- [ ] 화상 면접 기능
- [ ] AI 이력서 첨삭 서비스
- [ ] 커뮤니티 기능 (Q&A, 후기 공유)
- [ ] 알림 시스템 (푸시 알림)

### 11.2 성능 최적화
- [ ] 이미지 Lazy Loading
- [ ] 코드 스플리팅 최적화
- [ ] CDN 도입
- [ ] 캐싱 전략 개선
- [ ] 번들 크기 최적화

### 11.3 UX 개선
- [ ] 다크 모드 지원
- [ ] 접근성 개선 (WCAG 2.1)
- [ ] 로딩 스켈레톤 UI
- [ ] 애니메이션 개선
- [ ] 오프라인 지원 (PWA)

### 11.4 개발 프로세스
- [ ] 유닛 테스트 작성 (Jest, React Testing Library)
- [ ] E2E 테스트 (Playwright)
- [ ] CI/CD 파이프라인 개선
- [ ] 코드 리뷰 프로세스 정립
- [ ] Storybook 도입

### 11.5 보안 강화
- [ ] CSP (Content Security Policy) 설정
- [ ] XSS 방어 강화
- [ ] CSRF 토큰 적용
- [ ] 민감 정보 암호화
- [ ] 보안 취약점 정기 점검

---

## 12. 참고 문서

### 12.1 기술 문서
- [React 공식 문서](https://react.dev/)
- [TypeScript 공식 문서](https://www.typescriptlang.org/)
- [Vite 공식 문서](https://vitejs.dev/)
- [React Router 공식 문서](https://reactrouter.com/)
- [Axios 공식 문서](https://axios-http.com/)

### 12.2 라이브러리 문서
- [Bootstrap 공식 문서](https://getbootstrap.com/)
- [Chart.js 공식 문서](https://www.chartjs.org/)
- [React Toastify](https://fkhadra.github.io/react-toastify/)
- [jsPDF](https://github.com/parallax/jsPDF)

### 12.3 배포 관련
- [Firebase Hosting 문서](https://firebase.google.com/docs/hosting)
- [Firebase CLI 문서](https://firebase.google.com/docs/cli)

### 12.4 내부 문서
- API 명세서: `docs/API.md`
- 디자인 가이드: `docs/DESIGN.md`
- 컨벤션: `docs/CONVENTIONS.md`

---

## 13. 첨부 자료

### 13.1 주요 설정 파일

#### `package.json`
```json
{
  "name": "jobkok-web",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "deploy": "npm run build && firebase deploy --only hosting"
  }
}
```

#### `vite.config.ts`
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react(), injectBuildDate()],
  resolve: {
    alias: {
      '@': '/src',
      '@components': '/src/shared/components',
    },
  },
})
```

#### `tsconfig.json`
```json
{
  "compilerOptions": {
    "strict": false,
    "skipLibCheck": true,
    "noImplicitAny": false,
    "resolveJsonModule": true,
    "esModuleInterop": true
  }
}
```

#### `firebase.json`
```json
{
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
```

### 13.2 주요 환경 변수

```typescript
// src/config/config.ts
export const IsDev = false;
export const API_BASE_URL = "https://api.jobkok.kr";
export const AI_BASE_URL = "https://ai.api.jobkok.kr";
export const NAVER_CLIENT_ID = "SXBr96y1InAJo4dJfnCV";
export const KAKAO_REST_API_KEY = "aff22010dc842b57d737fc236ba46676";
export const GOOGLE_CLIENT_ID = "437460955925-erd8t8c78c8h3hh57gr9552m6pc5gtq7...";
```

### 13.3 프로젝트 통계

| 항목 | 내용 |
|------|------|
| **총 개발 기간** | 2024년 8월 ~ 2025년 5월 (10개월) |
| **총 코드 라인** | 약 74,642 라인 |
| **주요 페이지 수** | 30+ 페이지 |
| **API 엔드포인트** | 50+ 개 |
| **컴포넌트 수** | 100+ 개 |
| **패키지 의존성** | 18개 (dependencies) |

---

## 14. 개발팀 정보

- **프로젝트 관리자**: [담당자명]
- **프론트엔드 개발**: [개발자명]
- **백엔드 개발**: [개발자명]
- **AI 개발**: [개발자명]
- **디자인**: [디자이너명]

---

## 15. 라이선스

- **프로젝트**: Private
- **사용 라이브러리**: 각 라이브러리의 라이선스 준수

---

## 16. 연락처

- **이메일**: support@jobkok.kr
- **웹사이트**: https://jobkok.kr
- **GitHub**: [저장소 URL]

---

**문서 작성일**: 2026년 7월 1일  
**문서 버전**: 1.0  
**최종 수정일**: 2026년 7월 1일
